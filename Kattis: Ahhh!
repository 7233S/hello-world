#include <iostream>
#include <string>
using namespace std;

int main() {
    string a, b;
    cin >> a >> b;
    
    if (a.size() >= b.size()) {
        cout << "yes";
    }
    else {
        cout << "no";
    } 
    
    return 0;
}

// Kattis Problem: Ahhh! Difficulty 1.5
